syntax = "proto3";
package utils;

option go_package = "github.com/SashwatAnagolum/picodb/utils";

import "utils/messagetypes.proto";

service PicoDBObserver {
    rpc Notify(PicoDBServerMessage) returns (PicoDBServerMessage);
}